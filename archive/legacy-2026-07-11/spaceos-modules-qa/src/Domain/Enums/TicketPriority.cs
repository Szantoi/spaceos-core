namespace SpaceOS.Modules.QA.Domain.Enums;

/// <summary>
/// Ticket priority
/// </summary>
public enum TicketPriority
{
    Critical = 0,   // Kritikus (gyártás leállítva)
    High = 1,       // Magas
    Medium = 2,     // Közepes
    Low = 3         // Alacsony
}
