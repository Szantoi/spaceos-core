namespace SpaceOS.Modules.QA.Domain.Enums;

/// <summary>
/// Priority levels for tickets (borrowed from CRM module).
/// </summary>
public enum CrmTaskPriority
{
    Low = 0,
    Medium = 1,
    High = 2,
    Critical = 3
}
