---
id: TASK-18-05
title: "FTS5 indexing for highlight keyword search"
type: task
epic: EPIC-18
scope: mcp-maintenance
status: pending
priority: P1
role: backend_developer
created: 2026-03-13
depends_on: [TASK-18-01]
blocks: [TASK-18-03, TASK-18-08]
fsm_state: "BACKLOG_READY"
fsm_retry_count: 0
estimated_effort: 2
---

# TASK-18-05: highlights_fts index

## Goal

Add FTS5-based keyword search for highlights (`key_decisions`, `lessons`, `next_steps`) to complement semantic retrieval.

## Affected Files

- `src/metadata/migrations/010_epic18_highlights_fts.sql` — NEW
- `src/mcp/AgentDb.ts` — add `searchHighlightsFts()`
- `src/tests/unit/highlights-fts.test.ts` — NEW

## Acceptance Criteria

Given multiple stored highlights,
When keyword search executes,
Then:

- AC-1: `highlights_fts` virtual table exists and is queryable.
- AC-2: Query by lesson keyword returns matching highlights.
- AC-3: Query by next-step phrase returns matching highlights.
- AC-4: Search returns in <100ms for 1000+ highlights (benchmark test).
- AC-5: Empty/no-match query returns empty list, not error.
- AC-6: Unit tests are green.

## Definition of Done

- [ ] FTS migration created and loaded
- [ ] AgentDb FTS query method implemented
- [ ] Benchmark-style test added (non-flaky threshold)
- [ ] Unit tests cover match + no-match + malformed query cases
- [ ] No TypeScript errors
- [ ] Code review approved

## Implementation Hints

- Keep content source aligned with `episode_highlights` rows.
- Use normalized lower-case searchable payloads for deterministic behavior.
- Guard against invalid MATCH syntax by sanitizing user query.

## Risks

- R1: FTS syntax errors from raw user input.
  - Mitigation: sanitize special operators and fallback to safe token search.
- R2: Drift between base table and virtual table.
  - Mitigation: define sync/update path in one repository method.
