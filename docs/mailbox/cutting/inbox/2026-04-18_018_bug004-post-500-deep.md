---
id: MSG-CUTTING-018
from: root
to: cutting
type: task
priority: high
status: READ
ref: MSG-TESTER-010
created: 2026-04-18
---

# BUG-004 POST /bff/cutting/plans még mindig 500

A TESTER reteszten (2026-04-18 10:02, `a60fcb4` deploy után):
- `GET /bff/cutting/plans` → **200** ✅ (javult a60fcb4-gyel)
- `POST /bff/cutting/plans` → **500** ❌ (még mindig 500)

Az E2E `51-cutting-plans-crud` tesztet viszont 201-el ment át korábban (`d91ce53` alatt). Ez arra utal, hogy a POST handler logikájában van hiba, nem a migrációban.

## Vizsgálandó

### 1. Cutting service log a POST-hoz

```bash
journalctl -u spaceos-cutting-svc --since "1 hour ago" | grep -i "error\|exception\|unhandled" | tail -30
```

### 2. POST handler — mi dob 500-at?

`src/SpaceOS.Modules.Cutting.Api/Endpoints/CuttingPlanEndpoints.cs` (vagy hasonló) — a `POST /api/cutting/plans` handler:
- Van-e `try/catch` ami lenyeli a hibát?
- Az `IDateTimeProvider`, `ICuttingRepository`, vagy valamilyen domain service dob-e?
- Az `InsertAsync` tranzakció sikeres-e?

### 3. BFF → Cutting proxy payload

`spaceos-orchestrator/src/routes/cutting.route.ts` — a BFF mit küld a Cutting service-nek?
- Mező nevek: `name` + `date`? Vagy más?
- Content-Type: `application/json` megy?

### 4. RLS / tenant context

A `TenantSessionInterceptor` megfelelően beállítja-e a GUC-ot a POST kéréshez?

## DoD

- [ ] Root cause azonosítva
- [ ] `POST /bff/cutting/plans { name, date }` → **201** VPS-en
- [ ] `dotnet test` → ≥77 zöld
- [ ] Ha INFRA deploy kell: jelezd BLOCKED-ban

---

*Skill: `/spaceos-terminal`*
