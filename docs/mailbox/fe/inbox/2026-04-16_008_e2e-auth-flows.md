---
id: MSG-FE-008
from: root
to: fe
type: task
priority: high
status: READ
ref: FE-PORTAL
created: 2026-04-16
---

# FE-008 — E2E-L2: Auth + layout flows (01-login, 08-auth-edge, 09-responsive)

## Kontextus

WD: `/opt/spaceos/spaceos-doorstar-portal/`. CLAUDE.md kötelező olvasás.
Pipeline: INBOX READ → CODE → BUILD → TEST → REVIEW → SECURITY → OUTBOX

## Előzmény

FE-007 DONE: Playwright scaffold kész (config + global-setup stub + auth.fixture.ts + 01-login skeleton).
Commit: `3a0b931`. Tesztek: 90/90 zöld.

Ref: `docs/tasks/active/FE-TEST-STRATEGY_doorstar-portal-test-infra.md` §5.4 (flow lista)

## Feladat

Az alábbi 3 E2E flow implementálása — ezek **nem igényelnek** `POST /bff/test/reset` endpointot,
mert nincs seed-specifikus adatfüggőségük. Futtathatók a live portalen (https://portal.joinerytech.hu).

**Környezet:** `.env.e2e.local` fájl (ne commit-old), tartalmazza:
```
TEST_USER_EMAIL=test-doorstar@spaceos.dev
TEST_USER_PASSWORD=<INFRA-124 outbox-ból>
VITE_API_BASE_URL=https://portal.joinerytech.hu
TEST_BFF_ENABLED=false
```

---

## Flow 01 — Login (`tests/e2e/flows/01-login.spec.ts`) — BŐVÍTÉS

A scaffold 2 stubja most valódi tesztek lesznek:

```typescript
import { test, expect } from '../fixtures/auth.fixture';

test.describe('01 — Login flow', () => {
  test('sikeres PKCE login → /orders dashboard betölt', async ({ authenticatedPage: page }) => {
    await expect(page).toHaveURL(/\/orders/);
    await expect(page.getByTestId('app-shell')).toBeVisible();
    await expect(page.getByRole('heading', { name: /rendelések/i })).toBeVisible();
  });

  test('bejelentkezett user látja az AppHeader navigációt', async ({ authenticatedPage: page }) => {
    await expect(page.getByRole('link', { name: /rendelések/i })).toBeVisible();
    await expect(page.getByRole('link', { name: /profil/i })).toBeVisible();
  });

  test('profile oldal betölt bejelentkezett usernek', async ({ authenticatedPage: page }) => {
    await page.goto('/profile');
    await expect(page.getByTestId('profile-name')).toBeVisible();
    await expect(page.getByRole('button', { name: /kijelentkezés/i })).toBeVisible();
  });

  test('kijelentkezés → Keycloak login oldalra redirect', async ({ authenticatedPage: page }) => {
    await page.goto('/profile');
    await page.getByRole('button', { name: /kijelentkezés/i }).click();
    await page.waitForURL(/auth\.joinerytech\.hu/, { timeout: 15_000 });
  });
});
```

---

## Flow 08 — Auth edge cases (`tests/e2e/flows/08-auth-edge.spec.ts`)

```typescript
import { test, expect } from '@playwright/test';  // nem auth fixture — publikus route-ok

test.describe('08 — Auth edge cases', () => {
  test('nem bejelentkezett user → /orders → Keycloak login oldalra redirect', async ({ page }) => {
    await page.goto('/orders');
    await page.waitForURL(/auth\.joinerytech\.hu/, { timeout: 15_000 });
    await expect(page).toHaveURL(/auth\.joinerytech\.hu/);
  });

  test('404 ismeretlen route → NotFoundPage jelenik meg', async ({ page }) => {
    // 404 oldal publikus — nem kell auth
    await page.goto('/this-route-does-not-exist');
    // Ha a ProtectedRoute van előtte, Keycloak redirect jön — elfogadható
    // Ha a NotFoundPage publikus: az 'Az oldal nem található' szöveg jelenik meg
    const isNotFound = await page.getByText(/az oldal nem található/i).isVisible().catch(() => false);
    const isKeycloak = page.url().includes('auth.joinerytech.hu');
    expect(isNotFound || isKeycloak).toBe(true);
  });

  test('direct navigáció /profile-ra nem bejelentkezve → auth redirect', async ({ page }) => {
    await page.goto('/profile');
    await page.waitForURL(/auth\.joinerytech\.hu/, { timeout: 15_000 });
    expect(page.url()).toContain('auth.joinerytech.hu');
  });
});
```

---

## Flow 09 — Responsive layout (`tests/e2e/flows/09-responsive.spec.ts`)

```typescript
import { test, expect } from '../fixtures/auth.fixture';

test.describe('09 — Responsive layout', () => {
  test('desktop nézet — AppHeader teljes szélességben látható', async ({ authenticatedPage: page }) => {
    await page.setViewportSize({ width: 1280, height: 800 });
    await expect(page.getByTestId('app-header')).toBeVisible();
    await expect(page.getByRole('link', { name: /rendelések/i })).toBeVisible();
  });

  test('mobil nézet (375px) — oldal görgethetően megjelenik', async ({ authenticatedPage: page }) => {
    await page.setViewportSize({ width: 375, height: 812 });
    await page.goto('/orders');
    await expect(page.getByTestId('app-shell')).toBeVisible();
    // Overflow-x ellenőrzés: ne legyen vízszintes scroll
    const hasHorizontalScroll = await page.evaluate(
      () => document.documentElement.scrollWidth > document.documentElement.clientWidth
    );
    expect(hasHorizontalScroll).toBe(false);
  });
});
```

---

## data-testid szükségletek

Ha a következő `data-testid` attribútumok még nem léteznek a FE kódban, add hozzá:

| Komponens | data-testid |
|---|---|
| App root div / fő wrapper | `app-shell` |
| AppHeader `<header>` elem | `app-header` |
| ProfilePage névmező | `profile-name` |

---

## DoD

- [ ] `tests/e2e/flows/01-login.spec.ts` — 4 valódi teszt (nem stub)
- [ ] `tests/e2e/flows/08-auth-edge.spec.ts` — 3 teszt
- [ ] `tests/e2e/flows/09-responsive.spec.ts` — 2 teszt
- [ ] `data-testid` attribútumok hozzáadva ahol hiányoznak
- [ ] `pnpm test:e2e` → 01 + 08 + 09 flow-k futnak, **≥7 E2E teszt zöld**
- [ ] `pnpm test` (unit + contract) → változatlanul 90/90 zöld
- [ ] `pnpm build` → 0 error
- [ ] `pnpm lint` + `pnpm typecheck` → 0 hiba
- [ ] git commit + push

## Megjegyzés — flow 06 + 02-05

A seed adatfüggő flow-k (02-cutting-dashboard, 03-daily-plan, 04-nesting, 05-inventory, 06-order-flow, 07-procurement) csak akkor jönnek, ha az Orchestrator BE-TEST-01 + BE-TEST-07 (seed profiles) DONE. Ez FE-009-ben lesz kiadva, amint a backend test infrastruktúra kész.

## Outbox

DONE: `mailbox/fe/outbox/2026-04-16_008_e2e-auth-flows-done.md`

## Skillек & Agentек

- `/senior-frontend` — Playwright auth fixture, data-testid konvenciók
- `/senior-qa` — auth redirect edge case, 404 ambiguity (ProtectedRoute vs NotFoundPage), viewport testing
- `/a11y-audit` — Playwright keyboard nav tesztek kiegészítésként
- Agent: `playwright-tester` — Playwright page.waitForURL, fixture lifecycle, expect.toHaveURL
- Agent: `expert-react-frontend-engineer` — data-testid hozzáadása komponensekhez
- Sub-agenteket nyugodtan indíts párhuzamosan (pl. 01-login + 08-auth-edge egyszerre, data-testid külön)
