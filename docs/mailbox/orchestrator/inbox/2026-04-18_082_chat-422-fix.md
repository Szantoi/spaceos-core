---
id: MSG-ORCH-082
from: root
to: orchestrator
type: task
priority: high
status: READ
ref: MSG-TESTER-005
created: 2026-04-18
---

# Chat 422 — `POST /bff/chat` (BUG-005)

## Tünet

Manuális teszt (`test-admin` user, `https://joinerytech.hu/chat`):
- Üzenet beírva, Send → `422 Unprocessable Entity` @ `POST /bff/chat`
- UI üzenet: `⚠️ Error: Something went wrong. Please try again.`
- Tenant context comboboxban **nincs Doorstar tenant** — csak E2E és `test-debug` látszik

## Valószínű okok

1. **Hiányzó tenant/facility kontextus** — a chat handler `tenantId` vagy `facilityId` nélkül 422-t ad. Ha a test-admin usernek nincs Doorstar tenantja a JWT claim-ekben (vagy a DB-ben nincs facility hozzárendelve), a kérés elutasítva.
2. **Tenant lista szűrés** — a chat tenant combobox valamilyen szűréssel jeleníti meg a tenantokat, ami kizárja a Doorstar bérlőt.
3. **OpenAI/LLM provider konfig** — ha az `OPENAI_API_KEY` nincs beállítva az Orchestratoron → 422 (provider error → unprocessable).

## Vizsgálandó

```bash
# Orchestrator log
journalctl -u spaceos-orchestrator -n 50 --no-pager | grep -E "422|chat|error"

# Vagy pm2
pm2 logs spaceos-orchestrator --lines 50
```

1. Mi a 422 hibaüzenet body-ja? (`details`/`error` mező a válaszban)
2. `/bff/chat` handler — milyen mezőket vár? (`tenantId`, `facilityId`, `message`, stb.)
3. A tenant lista — honnan jön? Ha Kernel `/api/tenants`-ból → miért nem szerepel Doorstar?

## DoD

- [ ] `POST /bff/chat` `test-admin` userrel 200-at ad vissza és AI választ ad
- [ ] Doorstar tenant megjelenik a chat tenant kontextus listában, VAGY dokumentálva van miért nem (pl. test-admin user nincs Doorstar tenanthoz rendelve → INFRA fix kell)
- [ ] Ha LLM konfig a probléma → jelezd BLOCKED-ban Root-nak

---

*Skill: `/spaceos-terminal`*
