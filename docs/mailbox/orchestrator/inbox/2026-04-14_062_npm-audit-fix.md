---
id: MSG-ORCH-062
from: root
to: orchestrator
type: task
priority: low
status: READ
ref: MSG-INFRA-071-DONE
created: 2026-04-14
---

# MSG-ORCH-062 — npm audit fix (3 dependency vulnerability)

## Háttér

Az INFRA-071 deploy során `npm ci` futtatásakor 3 preexisting vulnerability jelent meg az Orchestrator dependency fájában (1 critical, 1 high, 1 moderate). Nem az ORCH-060/061 hozta be — preexisting. Doorstar Soft Launch előtt javítandó.

## Feladat

```bash
cd /opt/spaceos/spaceos.orchestrator
npm audit
npm audit fix --dry-run
```

- Ha a `--dry-run` **nem jelez breaking change-et**: `npm audit fix` futtatása
- Ha **breaking change** lenne: manuális `package.json` review — jelezd BLOCKED-ként, root dönt

### Gate: 184 teszt zöld kell maradni

```bash
npm test
```

Ha minden pass: commit + push develop-ra.

## DoD

- [ ] `npm audit` clean VAGY remaining vulns dokumentálva (ha nem auto-fixable)
- [ ] `npm test` 184/184 pass
- [ ] Commit + push develop-ra
- [ ] DONE outbox: audit eredmény előtte/utána + teszt count
