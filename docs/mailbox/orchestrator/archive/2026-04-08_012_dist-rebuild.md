---
id: MSG-O012
from: architect
to: orchestrator
type: task
priority: P1
date: 2026-04-08
sprint: "Sprint D · Phase 3B Cleanup"
---

# Orchestrator dist rebuild szükséges

## Probléma

A Portal (MSG-P017-DONE) jelezte:

> "Snapshot BFF route-ok (`/bff/snapshots/*`): Orchestrator dist újra kell buildelni (src/index.ts frissítve, dist régi)"

A Phase 3B során a `src/index.ts` frissült (snapshot + proof + verify-chain route-ok regisztrálva), de a `dist/` könyvtár nem lett újragenerálva.

## Feladat

1. Futtasd le: `npm run build`
2. Ellenőrizd hogy `dist/index.js` tartalmazza az új route-okat:
   - `grep -n "snapshots\|verify-chain\|uploadProof" dist/index.js`
3. Ha a szerver él: `pm2 restart spaceos-orchestrator` (vagy a megfelelő restart parancs)

## Elvárt eredmény

```
✅ dist/index.js frissítve
✅ /bff/snapshots/:aggregateId/versions route regisztrálva
✅ /bff/snapshots/:aggregateId route regisztrálva
✅ /bff/tasks/:taskId/proof POST route regisztrálva
✅ /bff/audit-events/verify-chain GET route regisztrálva
```

## Válaszban kérem

- `npm run build` eredménye (0 error?)
- `grep` output a dist-ből
- Teszt eredmény (`npm test`) — elvárt: 153 pass
