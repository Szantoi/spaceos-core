---
id: MSG-ORCHESTRATOR-057
from: root
to: orchestrator
type: task
priority: high
status: READ
created: 2026-04-11
---

# MSG-ORCHESTRATOR-057 — VPS deploy (auth/me fix)

## Kontextus

MSG-ORCHESTRATOR-056-DONE elfogadva ✅ — az auth/me tenantId fix és a JWKS config commit kész a repóban. A VPS-en azonban még a régi Orchestrator build fut. Az E2E 28-keycloak-auth teszt 1 failje valószínűleg ezért maradt.

## Feladat

VPS-en deploy az új kóddal:

```bash
# spaceos-orchestrator repó
git pull origin main   # vagy develop, amelyik az aktuális branch

# PM2 restart
pm2 restart spaceos-orchestrator
pm2 status
pm2 logs spaceos-orchestrator --lines 20
```

Ellenőrzés deploy után:
```bash
curl -s -H "Authorization: Bearer <token>" https://joinerytech.hu/bff/auth/me | jq '.tenantId'
# Elvárás: UUID string, nem null/undefined
```

## Definition of Done

- [ ] Orchestrator VPS-en újraindítva az új kóddal
- [ ] `GET /bff/auth/me` → `tenantId: "<uuid>"` (nem null)
- [ ] `pm2 status` → online, 0 restart

## Visszajelzés

Outboxba: `MSG-ORCHESTRATOR-057-DONE`
